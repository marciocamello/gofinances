const dataKey = '@gofinances:transactions';

const defaultCategory = {
    key: 'category',
    name: 'Category',
};

const currency = 'EUR';
const language = 'en-US';

export {
    dataKey,
    defaultCategory,
    currency,
    language,
}